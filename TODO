o:5 — старые 5
n:3 — новые 3
y:2 — моложе двух дней
e:3 — старше трёх дней
w:1-7 — указанный день недели
d — удалить
k — оставить (удалить все остальные)
m:\\srv-2\backups\ — переместить в назначение ??
c:\\srv-2\backups\ — копировать в назначение

Будущие релизы:
Выбор файлов по маске

keys, operations:
d, f, df - what to select in current path (directories, files, all) - getfiles
r, k, c:_ - what to perform (remove, keep, copy to _) - processfiles
o:_, n:_ - selection masks (oldest _, newest _) - filterfiles
w:_ - selection mask (weekday [1..7]) - filterfiles

check for end-slash in path
copy-path cannot contain "disk:" on Windows !!
can only copy files